# Used only by api/token.ts (server-side, never exposed to the browser).
# Copy to .env for local `vercel dev`, and set the same values in the
# Vercel project's Environment Variables for production.

LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

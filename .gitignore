# Python
agent/.venv/
__pycache__/
*.pyc
agent/.env.local

# Node / Angular
web/node_modules/
web/dist/
web/.angular/
web/.env

# LiveKit Cloud agent config (generated on deploy, may contain project-specific ids)
# keep livekit.toml/agent.toml tracked once generated, unless it embeds secrets

# Private local planning notes — not part of this project's public description.
PROJECT_BRIEF.md

# OS
.DS_Store
